🎉 WordPress deployed for client: {{ .Values.clientName }}

📍 Access your site at:
   https://{{ .Values.clientDomain }}

📊 Architecture (per-client isolated):
   - WordPress: PHP 8.3 + Apache
   - MariaDB: Dedicated database (sidecar)
   - Valkey: Redis-compatible cache (sidecar)
   - Plan: {{ .Values.plan }}

🔧 First-time setup:
   1. Wait for pod to be ready (MariaDB init takes ~30-60s)
   2. Visit https://{{ .Values.clientDomain }}
   3. Complete the WordPress installation wizard
   4. Install "Redis Object Cache" plugin for caching

📦 Check status:
   kubectl get pods -n {{ .Release.Namespace }}
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Values.clientName }} -c wordpress

💾 Backup (entire client data):
   # Backup both PVCs for complete restore capability
   kubectl get pvc -n {{ .Release.Namespace }}

🗑️ Uninstall:
   helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
   kubectl delete namespace {{ .Release.Namespace }}
   
   ⚠️  This will DELETE all client data (WordPress files + database)!
